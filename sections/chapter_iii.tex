\newpage
\addcontentsline{toc}{chapter}{\quad \: III. \hspace{5pt} IMPLEMENTATION}

\begin{center}
  \vspace*{1.65in}
  CHAPTER III\\
  IMPLEMENTATION
\end{center}

% background intro
\bigskip

This work studies spectrum sensing for a prospective CubeSat cognitive radio (CR).  Within the deliberately limited M-QAM-SRRC model used in this thesis, the receiver searches a specified grid of candidate symbol rates (SRs) and center frequencies (CFs) and uses cyclostationary feature detection (CFD) to distinguish modeled signals from noise.  A spectral correlation analyzer (SCA) can realize this detector.  Conventional FFT-based SCAs obtain high throughput by evaluating many spectral channels in parallel.  Their implementations buffer blocks of complex samples and intermediate transform results, so storage grows with both the capture length and the number of retained channels.  This memory can compete with limited on-chip FPGA block RAM (BRAM) in a CubeSat-class implementation.

BRAM capacity is therefore a central design constraint.  Reducing buffered storage improves fit within on-chip memory, while sample-by-sample time-domain processing trades the parallel throughput of an FFT-based analyzer for a deeply pipelined data path.  To limit the resulting search time, the application is restricted to detecting the SR and CF of M-QAM-SRRC waveforms rather than estimating every cyclic feature of an arbitrary waveform.  For this blind SR--CF estimator, we borrow the communications term \textit{streaming}.  Thus, this design will be referred to as the \textit{streaming SCA}.  It is an FPGA architecture study, not a flight-qualified implementation.


\clearpage
%--------------------------------------------------------
%
% 3.1
%
\bigskip
\addcontentsline{toc}{section}{\quad \: 3.1 \quad Motivation}
\noindent 3.1  \quad Motivation

Orbital systems may use a single spacecraft, a constellation, or a distributed architecture such as a fractionated, clustered, or trailing formation.  Distributed architectures coordinate multiple spacecraft, and CubeSats are one relevant small-spacecraft platform \cite{bandyopadhyay-15}.

Crosslinks and relays create operating scenarios in which inter-satellite interference may need to be monitored.  Spectrum sensing is one candidate mechanism for detecting occupied channels and informing an interference-avoidance response.

As discussed in the previous chapter, SCAs estimate cyclic spectral structure that can be used for spectrum sensing \cite{spooner-92}.  Several efficient digital SCA realizations use FFTs, sample blocks, and intermediate arrays whose storage requirements can be substantial \cite{spooner-92,liang-20}.

For a concrete scaling example, an SSCA intermediate array with $N=2^{16}$ time samples and $N_p=32$ retained frequency channels contains $NN_p$ complex words \cite{spooner-92}.  At $16$ bits for each in-phase and quadrature component, or $32$ bits per complex word, that array alone requires $2^{16}\times32\times32=67{,}108{,}864$ bits, or $67.1\text{ Mbit}$, before control and buffering overhead.  The recovered 2021 component design reports and project-recreation scripts consistently identify the intended development platform as a ZedBoard with an XC7Z020CLG484-1.  That device provides $4.9\text{ Mbit}$ of block RAM \cite{xilinx-ds190}, so this particular array would not fit wholly in its BRAM without reducing the stored data or changing the architecture.  This establishes the documented component-development target; it does not establish the provenance of the experiment-level resource counts in Chapter IV, whose top-level synthesis report is unavailable.

% 3.2
%
\bigskip
\addcontentsline{toc}{section}{\quad \: 3.2 \quad The Streaming SCA}
\noindent 3.2  \quad The Streaming SCA

Let us assume that we wish to sense modeled waveforms present in a channel.  As discussed in Chapter II, the single-term CAF-DFT form can evaluate selected frequency candidates without a block FFT.  To create a low-memory design suitable for evaluation on the selected FPGA, we alter the CAF-DFT such that it:

\begin{itemize}
\item Evaluates only one pipelined $(v,k)$ pair at a time, called a point.  This is equivalent to one branch in Figure~\ref{fig:koch_sca}.  If our SCA is well designed, the branches can be reconfigured so that multiple instances can be used in parallel when the platform has extra resources.

\item Avoids a full-capture BRAM buffer.  A block-based realization buffers a capture so that every $(v,k)$ pair is evaluated on the same samples.  This design instead accepts a continuous input sample stream without retaining the full capture; ADC interfacing is outside the module.  Consequently, different candidate pairs can observe different time intervals, but the design reduces buffered storage when the signal is sufficiently stable over the search.

\item Targets FPGA implementation so that the estimator can use deterministic pipelines and configurable parallelism within the platform's resource budget.  FPGA implementation alone does not establish radiation tolerance; device qualification and mitigation are separate design considerations.

\item Does not evaluate the zero-cycle-frequency case $\alpha=0$, which corresponds to the ordinary PSD rather than a nontrivial cyclostationary feature.

\item Uses a fixed two-tap low-pass filter (LPF), corresponding to taps $[0.5,0.5]$.  Koch's evaluated carrier-frequency detector systematically shifts each segment, dynamically configures the LPF bandwidth to the target symbol rate, and evaluates the CAC at that rate \cite{koch-19}.  The streaming design is a serial hardware adaptation of that sweep; to avoid a reconfigurable coefficient bank, it uses the same two coefficients for every candidate.
\end{itemize}

\begin{figure}[H]
\begin{center}
\includegraphics[scale=0.75]{plots/qam_srrc_rs}
\caption[Streaming SCA detections for two M-QAM-SRRC signals.]{Streaming SCA test statistic $T_y(v,k)=|\widehat{C}_{y}(\alpha_v,f_k)|^2$ for two M-QAM-SRRC signals in a shared noise floor.  The transmitted parameter pairs are $(R_s,f_c)=(100\text{ kBd},50\text{ kHz})$ and $(200\text{ kBd},100\text{ kHz})$.  Both pairs lie on the estimator grid, both row maxima recover the transmitted CF, and both exceed the constant threshold $\gamma=\beta\widehat{P}_{y}^{2}$ with $\beta=10^{-3}$.  Green markers indicate the detected row maxima.}
\label{fig:qam_srrc_rs}
\end{center}
\end{figure}

The streaming SCA was modeled in MATLAB to produce Figure~\ref{fig:qam_srrc_rs}.  The two generated components are scaled so that a single receiver-noise realization gives the requested $E_b/N_0$ values of $14$ dB and $9$ dB.  A common automatic-gain-control (AGC) scale factor then limits the composite waveform without changing either component's carrier-to-noise ratio.  On the $10$ kHz CF grid, the row maxima occur at the transmitted CFs of $50$ kHz and $100$ kHz; their test-statistic-to-threshold ratios are $9.41$ and $2.16$, respectively.  These numerical values are reproducible because the model fixes its random-number seed.  The executable MATLAB model is imported directly in Appendix A.

In exchange for this low memory use, the processing time is increased.  With one $(v,k)$ point evaluated at a time, no backpressure, and a sustained rate of one valid sample per clock, an exhaustive search over $N_v$ SR candidates and $N_k$ CF candidates requires approximately $NN_vN_k$ sample clocks, in addition to pipeline and control overhead.  For fixed search spans, the processing delay therefore increases with capture length and with the number of candidates:

\begin{itemize}
\item SR step size: A smaller step produces more SR candidates and a longer search, whereas a larger step coarsens the grid and can increase parameter mismatch.

\item CF step size: A smaller step produces more CF candidates and a longer search, whereas a larger step coarsens the grid and can increase parameter mismatch.

\item Capture length $N$: A larger capture takes longer to process but provides a longer observation interval; a smaller capture reduces delay at the cost of a less stable finite-record estimate.
\end{itemize}

%---------------------------------------------------------
%
% 3.3
%
\bigskip
\addcontentsline{toc}{section}{\quad \: 3.3 \quad Digital Design}
\noindent 3.3  \quad Digital Design

To describe the implementation of the streaming SCA as a digital circuit, we will walk through each component and its subcomponents.  The documented signal-processing datapath uses custom VHDL modules rather than a vendor FFT core.  MATLAB and Xilinx Vivado were nevertheless used for modeling and implementation, and inferred device resources can affect portability.  The tables summarize the configured documentation interface rather than reproducing every literal entity generic or port name; in particular, some propagation-only width and latency generics are omitted, and several arithmetic names are shortened.

The datapath uses a continuous-rate, fixed-latency streaming contract.  During each $N$-sample record, \texttt{SampleInValid} is asserted on every clock and downstream \texttt{SampleOutReady} is held high.  In sample-preserving stages, data and their valid and final-sample tags advance together at a fixed latency.  At an integrate-and-dump boundary, every accepted input contributes to the record state and one result, valid tag, and final-sample tag are emitted after the terminal input reaches that boundary.  The ready ports provide latency-aligned status under this contract.  A fully elastic variant would instead advance every sample-indexed state element only on \texttt{SampleInValid and SampleInReady} and would hold a pending output stable until \texttt{SampleOutReady} is asserted; that operating mode is outside the characterization in Chapter IV.

Surviving artifacts document two distinct configurations.  The contemporaneous CFE component report supplies the thesis configuration, in which \texttt{TAP\_COUNT=2} gives $h=[0.5,0.5]$.  The recovered July 2022 worktree supplies later-code evidence and contains an active 41-tap, 128-bank filter.  Descriptions of the thesis configuration below therefore follow the contemporaneous component documentation, while source-level observations are attributed separately to the later worktree.  A later top-level wrapper with an FFT-based symbol-rate preprocessor is outside the implementation characterized here.  The discussion occasionally refers to VHDL functions, such as \texttt{resize()}; however, the signal-processing description is otherwise language-agnostic.

%
% 3.3.1
%
\medskip
\addcontentsline{toc}{subsection}{\quad 3.3.1 \quad Center Frequency Estimator}
\noindent 3.3.1  \quad Center Frequency Estimator

% the purpose of the module
The purpose of the center frequency estimator (CFE) is to produce a test-statistic surface over the SR--CF candidates.  Let $v$ and $k$ index user-defined candidate arrays with physical frequencies $\alpha_v$ and $f_k$, respectively.  For an input sequence $x[n]$, define

\begin{equation*}
z_k[n]=\left[\left(x[\cdot]e^{-j2\pi f_k(\cdot)/F_s}\right)\ast h[\cdot]\right][n],
\qquad
\widehat{C}_{x}(\alpha_v,f_k)=\frac{1}{N}\sum_{n=0}^{N-1}|z_k[n]|^{2}e^{-j2\pi\alpha_v n/F_s}.
\end{equation*}

The center-frequency control word configures a downconverting local oscillator for the factor $e^{-j2\pi f_k n/F_s}$.  Next, in the thesis configuration, an LPF module smooths the mixed signal with the fixed taps $[0.5,0.5]$.  The SRE then evaluates the zero-lag CAF coefficient $\widehat{C}_{x}(\alpha_v,f_k)=\widehat{R}_{z_k}^{\alpha_v}[0]$, and the final magnitude-squared operation produces one point of the test statistic, $T_x(v,k)=|\widehat{C}_{x}(\alpha_v,f_k)|^{2}$.  Thus, $v$ and $k$ are candidate-array indices rather than frequencies themselves.  As explained in Chapter II, $\widehat{C}_{x}$ is a filter-localized cyclic statistic, not literally the CSD value at $f_k$.

For each candidate pair $(\alpha_v,f_k)$, the controller holds both frequency-control words constant for one $N$-sample record.  Each sample is downconverted by $f_k$, filtered by $h[n]$, converted to a nonnegative magnitude-squared value, mixed by $\alpha_v$, and accumulated.  The final sample is included exactly once in the record result.  After normalization by $N$, the magnitude squared of the complex sum forms $T_x(v,k)$.  After the preceding record boundary has traversed the fixed-latency pipeline, the controller supplies an inter-record invalid bubble that inserts zero into the FIR's one-sample delay.  It then applies the next candidate pair and accepts its first sample with zero FIR state.

The thesis configuration specializes the NCO to a 16-bit phase accumulator with two quadrant bits and a 14-bit quarter-wave lookup address.  An integer control word $q$ represents $qF_s/2^{16}$ modulo $F_s$, giving a $15.2588$-Hz quantum at $F_s=1\text{ MHz}$.  Signed center-frequency codes represent $[-F_s/2,F_s/2-F_s/2^{16}]$.  Unsigned symbol-rate codes 0 through 32767 retain their positive interpretation at the signed NCO boundary, while code 32768 represents the aliased Nyquist endpoint.  Under the continuous-rate contract, the oscillator advances one control-word step on every sample clock, so oscillator phase index and sample index advance together.  If the cycle-frequency candidates are restricted to an $N$-point DFT grid, MATLAB's one-based index satisfies $\alpha_v=(v-1)F_s/N$; the NCO can also evaluate candidates off that grid subject to its $15.2588$-Hz quantization.

\begin{figure}[H]
\begin{center}
\resizebox{\textwidth}{!}{\input{../images/diagrams/tikz/cfe.tex}}
\caption{Functional architecture of the CFE module. Solid arrows carry sampled data; dashed arrows carry configuration and block control.}
\label{fig:cfe}
\end{center}
\end{figure}

\begin{figure}[H]
\begin{center}
\resizebox{0.96\textwidth}{!}{\input{../images/diagrams/tikz/timing_cfe.tex}}
\caption{Symbolic continuous-rate timing of the CFE module; both control words remain fixed for each $N$-sample record, and one statistic is emitted after the terminal sample.}
\label{fig:8_CFE}
\end{center}
\end{figure}

\begin{table}[H]
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{center}
\caption{Configured CFE interface.}
\begin{tabularx}{\textwidth}{|L{0.24\textwidth}|L{0.13\textwidth}|L{0.28\textwidth}|Y|}
\hline
\textbf{Port Name} & \textbf{Direction} & \textbf{Data Type} & \textbf{Description} \\
\hline
N\_SAMPLES         & Generic & natural & Configured record length; \texttt{SampleInLast} marks sample $N-1$. \\
\hline
BIT\_WIDTH         & Generic & natural & Sample width: one sign bit and BIT\_WIDTH-1 fractional bits. \\
\hline
Clock              & Input & std\_logic & Synchronous clock. \\
\hline
Reset              & Input & std\_logic & Synchronous, active-high reset. \\
\hline
SampleOutReady     & Input & std\_logic & Downstream status input; held high during continuous-rate operation. \\
\hline
SampleInValid      & Input & std\_logic & High when input is valid. \\
\hline
SampleInLast       & Input & std\_logic & End-of-record tag asserted with the final input sample. \\
\hline
CenterFrequency\newline Fcw & Input & signed(15 downto 0) & Signed 16-bit CF control word. \\
\hline
SymbolRateFcw      & Input & unsigned(15 downto 0) & Unsigned 16-bit SR control word. \\
\hline
SampleIn\_I        & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Real component of sample. \\
\hline
SampleIn\_Q        & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Imaginary component of sample. \\
\hline
SampleInReady      & Output & std\_logic & Latency-aligned upstream status for the continuous-rate interface. \\
\hline
SampleOutValid     & Output & std\_logic & High when output is valid. \\
\hline
SampleOutLast      & Output & std\_logic & End-of-record tag aligned with the associated output. \\
\hline
OverflowStatus     & Output & std\_logic\_vector(3 \newline downto 0) & [3]: CF mixer I \newline [2]: CF mixer Q \newline [1]: SR mixer I \newline [0]: SR mixer Q \\
\hline
SampleOut          & Output & unsigned(\newline 2*BIT\_WIDTH-1 \newline downto 0) & Nonnegative magnitude-squared result. \\
\hline
\end{tabularx}
\end{center}
\end{table}

%
% 3.3.2
%
\bigskip
\addcontentsline{toc}{subsection}{\quad 3.3.2 \quad Symbol Rate Estimator}
\noindent 3.3.2  \quad Symbol Rate Estimator

% purpose
The purpose of the SR estimator (SRE) is to estimate the correlation between a waveform and the candidate SR cycle frequency $\alpha_v$.  Its computation is the finite-record CAF estimate evaluated at $\tau=0$:

\begin{equation*}
\widehat{R}_{X}^{\alpha_v}[0] = \frac{1}{N} \sum_{n=0}^{N-1} |X[n]|^{2} e^{-j2\pi\alpha_v n/F_s}.
\end{equation*}

The instantaneous-power module computes the nonnegative value $|X[n]|^{2}$, and the selected-frequency Fourier-sum module evaluates the displayed zero-lag CAF expression at $\alpha_v$.  For Q1.15 input components, $P=I^2+Q^2$ lies in $[0,2]$ and has a full-precision unsigned 32-bit representation with 30 fractional bits.  The format boundary into the signed Fourier mixer therefore preserves the magnitude bit by either zero-extending into a wider signed word or by explicitly rescaling and saturating.  One conforming 16-bit mapping is a saturated Q1.15 representation of $P/2$.  This scales the resulting complex CAF coefficient by one half and the downstream magnitude-squared statistic and matching threshold by one quarter; those factors are carried consistently.  Every power sample remains nonnegative throughout the interface.

\begin{figure}[H]
\begin{center}
\resizebox{\textwidth}{!}{\input{../images/diagrams/tikz/st_sre.tex}}
\caption{Functional architecture of the SRE module, including the nonnegative fixed-point format boundary after instantaneous power.}
\label{fig:sre}
\end{center}
\end{figure}

\begin{figure}[H]
\begin{center}
\resizebox{0.96\textwidth}{!}{\input{../images/diagrams/tikz/timing_sre.tex}}
\caption{Symbolic continuous-rate timing of the SRE module with an aligned terminal result, valid tag, and final-sample tag.}
\label{fig:7_SRE}
\end{center}
\end{figure}

\begin{table}[H]
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{center}
\caption{Configured SRE interface.}
\begin{tabularx}{\textwidth}{|L{0.24\textwidth}|L{0.13\textwidth}|L{0.28\textwidth}|Y|}
\hline
\textbf{Port Name} & \textbf{Direction} & \textbf{Data Type} & \textbf{Description} \\
\hline
N\_SAMPLES         & Generic & natural & Configured record length; \texttt{SampleInLast} marks sample $N-1$. \\
\hline
BIT\_WIDTH         & Generic & natural & Sample width: one sign bit and BIT\_WIDTH-1 fractional bits. \\
\hline
Clock              & Input & std\_logic & Synchronous clock. \\
\hline
Reset              & Input & std\_logic & Synchronous, active-high reset. \\
\hline
SampleOutReady     & Input & std\_logic & Downstream status input; held high during continuous-rate operation. \\
\hline
SampleInValid      & Input & std\_logic & High when input is valid. \\
\hline
SampleInLast       & Input & std\_logic & End-of-record tag asserted with the final input sample. \\
\hline
OscillatorFcw      & Input & unsigned(15 downto 0) & Unsigned 16-bit SR control word. \\
\hline
SampleIn\_I        & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Real component of sample. \\
\hline
SampleIn\_Q        & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Imaginary component of sample. \\
\hline
SampleInReady      & Output & std\_logic & Latency-aligned upstream status for the continuous-rate interface. \\
\hline
SampleOutValid     & Output & std\_logic & High when output is valid. \\
\hline
SampleOutLast      & Output & std\_logic & End-of-record tag aligned with the associated output. \\
\hline
OverflowStatus     & Output & std\_logic\_vector(1 \newline downto 0) & [1]: mixer I \newline [0]: mixer Q \\
\hline
SampleOut\_I       & Output & signed(BIT\_WIDTH-1 \newline downto 0) & Real component of result. \\
\hline
SampleOut\_Q       & Output & signed(BIT\_WIDTH-1 \newline downto 0) & Imaginary component of result. \\
\hline
\end{tabularx}
\end{center}
\end{table}

%
% 3.3.3
%
\bigskip
\addcontentsline{toc}{subsection}{\quad 3.3.3 \quad Discrete Fourier Transform}
\noindent 3.3.3  \quad Discrete Fourier Transform

% purpose
The DFT is used to perform frequency-domain analysis of discrete-time signals.  This implementation evaluates a finite-record Fourier sum at one selected physical frequency $f_0$ at a time:

\begin{equation*}
\widehat{X}(f_0)=\frac{1}{N}\sum_{n=0}^{N-1}X[n]e^{-j2\pi f_0n/F_s}.
\end{equation*}
	
The corresponding normalized $N$-point DFT coefficient $\widehat{X}[\ell]$ is the special case $f_0=\ell F_s/N$ for the zero-based bin $0\leq\ell<N$.  The oscillator frequency-control word selects $f_0$, the mixer forms the complex products $X[n]e^{-j2\pi f_0n/F_s}$, and separate real and imaginary accumulators sum exactly $N$ products.  The final marked sample is included before the completed sums are normalized, quantized, and emitted.  The quarter-wave lookup-table reconstruction can introduce a constant NCO initial-phase factor, but that factor cancels in the final magnitude squared.  For the SRE, $f_0=\alpha_v$; when the candidates lie on the DFT grid, $\ell=v-1$ under MATLAB's one-based indexing.

\begin{figure}[H]
\begin{center}
\resizebox{\textwidth}{!}{\input{../images/diagrams/tikz/st_dft.tex}}
\caption{Selected-frequency Fourier-sum architecture. Both accumulators include the final accepted sample before normalization.}
\label{fig:dft}
\end{center}
\end{figure}

\begin{figure}[H]
\begin{center}
\resizebox{0.96\textwidth}{!}{\input{../images/diagrams/tikz/timing_dft.tex}}
\caption{Symbolic continuous-rate timing of the selected-frequency DFT module; the normalized Fourier result follows the terminal input sample by fixed latency.}
\label{fig:6_DFT}
\end{center}
\end{figure}

\begin{table}[H]
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{center}
\caption{Configured DFT interface.}
\begin{tabularx}{\textwidth}{|L{0.24\textwidth}|L{0.13\textwidth}|L{0.28\textwidth}|Y|}
\hline
\textbf{Port Name} & \textbf{Direction} & \textbf{Data Type} & \textbf{Description} \\
\hline
N\_SAMPLES         & Generic & natural & Configured record length; \texttt{SampleInLast} marks sample $N-1$. \\
\hline
BIT\_WIDTH         & Generic & natural & Sample width: one sign bit and BIT\_WIDTH-1 fractional bits. \\
\hline
Clock              & Input & std\_logic & Synchronous clock. \\
\hline
Reset              & Input & std\_logic & Synchronous, active-high reset. \\
\hline
SampleOutReady     & Input & std\_logic & Downstream status input; held high during continuous-rate operation. \\
\hline
SampleInValid      & Input & std\_logic & High when input is valid. \\
\hline 
SampleInLast       & Input & std\_logic & End-of-record tag asserted with the final input sample. \\
\hline
OscillatorFcw      & Input & signed(15 downto 0) & Signed 16-bit frequency control word. \\
\hline
SampleIn\_I        & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Real component of sample. \\
\hline
SampleIn\_Q        & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Imaginary component of sample. \\
\hline
SampleInReady      & Output & std\_logic & Latency-aligned upstream status for the continuous-rate interface. \\
\hline
SampleOutValid     & Output & std\_logic & High when output is valid. \\
\hline
SampleOutLast      & Output & std\_logic & End-of-record tag aligned with the associated output. \\
\hline
OverflowStatus     & Output & std\_logic\_vector(1 \newline downto 0) & [1]: mixer I \newline [0]: mixer Q \\
\hline
SampleOut\_I       & Output & signed(BIT\_WIDTH-1 \newline downto 0) & Real component of result. \\
\hline
SampleOut\_Q       & Output & signed(BIT\_WIDTH-1 \newline downto 0) & Imaginary component of result. \\
\hline
\end{tabularx}
\end{center}
\end{table}

%
% 3.3.4
%
\bigskip
\addcontentsline{toc}{subsection}{\quad 3.3.4 \quad Frequency Mixer}
\noindent 3.3.4  \quad Frequency Mixer

% purpose
Frequency translation is performed by multiplying the input by a complex local oscillator generated by a numerically controlled oscillator (NCO).  For a positive-frequency component at $f_1$, multiplication by the downconverting oscillator at $f_{\mathrm{LO}}$ gives

\begin{equation*}
e^{j2\pi f_1n/F_s}\,e^{-j2\pi f_{\mathrm{LO}}n/F_s}
=e^{j2\pi(f_1-f_{\mathrm{LO}})n/F_s}.
\end{equation*}

When $f_{\mathrm{LO}}=f_1$, the component is translated to zero frequency.  The implementation generates the oscillator with a phase accumulator and a sine/cosine lookup table; the frequency-control word sets its phase increment.  Under the characterized continuous-rate contract, the phase accumulator advances once per sample clock and the phase index therefore remains aligned with the consecutive sample index $n$.  A fully elastic variant gates the phase update with the sample-accept event so that a stalled transaction preserves both the sample and its corresponding oscillator phase.

\begin{figure}[H]
\begin{center}
\resizebox{\textwidth}{!}{\input{../images/diagrams/tikz/mixer.tex}}
\caption{Functional architecture of the frequency mixer. The NCO phase advances once per sample clock under the continuous-rate contract.}
\label{fig:mixer}
\end{center}
\end{figure}

\begin{figure}[H]
\begin{center}
\resizebox{0.96\textwidth}{!}{\input{../images/diagrams/tikz/timing_mixer.tex}}
\caption{Symbolic continuous-rate timing of the frequency mixer module. Data, valid, and final-sample tags share the same fixed latency.}
\label{fig:5_GenMixer}
\end{center}
\end{figure}

\begin{table}[H]
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{center}
\caption{Configured frequency-mixer interface.}
\begin{tabularx}{\textwidth}{|L{0.24\textwidth}|L{0.13\textwidth}|L{0.28\textwidth}|Y|}
\hline
\textbf{Port Name} & \textbf{Direction} & \textbf{Data Type} & \textbf{Description} \\
\hline
BIT\_WIDTH         & Generic & natural & Sample width: one sign bit and BIT\_WIDTH-1 fractional bits. \\
\hline
Clock              & Input & std\_logic & Synchronous clock. \\
\hline
Reset              & Input & std\_logic & Synchronous, active-high reset. \\
\hline
SampleOutReady     & Input & std\_logic & Downstream status input; held high during continuous-rate operation. \\
\hline
SampleInValid      & Input & std\_logic & High when input is valid. \\
\hline
SampleInLast       & Input & std\_logic & End-of-record tag asserted with the final input sample. \\
\hline
OscillatorFcw      & Input & signed(15 downto 0) & Signed 16-bit frequency control word. \\
\hline
SampleIn\_I        & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Real component of sample. \\
\hline
SampleIn\_Q        & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Imaginary component of sample. \\
\hline
SampleInReady      & Output & std\_logic & Latency-aligned upstream status for the continuous-rate interface. \\
\hline
SampleOutValid     & Output & std\_logic & High when output is valid. \\
\hline
SampleOutLast      & Output & std\_logic & End-of-record tag aligned with the associated output. \\
\hline
OverflowStatus     & Output & std\_logic\_vector(1 \newline downto 0) & [1]: I \newline [0]: Q \\
\hline
SampleOut\_I       & Output & signed(BIT\_WIDTH-1 \newline downto 0) & Real component of mixed sample. \\
\hline
SampleOut\_Q       & Output & signed(BIT\_WIDTH-1 \newline downto 0) & Imaginary component of mixed sample. \\
\hline
\end{tabularx}
\end{center}
\end{table}

%
% 3.3.5
%
\smallskip
\addcontentsline{toc}{subsection}{\quad 3.3.5 \quad Low-Pass Filter}
\noindent 3.3.5  \quad Low-Pass Filter

% purpose
A finite impulse response (FIR) filter has a finite-duration impulse response.  Filter coefficients are calculated from the desired properties of the filter in MATLAB or Python.  An $L$-tap FIR implements the convolution sum

\begin{equation*}
Y[n]=\sum_{i=0}^{L-1}b[i]X[n-i].
\end{equation*}

Here, $b[i]$ are the $L$ coefficients, and $X[n]$ is the input sequence.  A causal FIR with $L$ taps has order $L-1$.  This design uses the two-tap, first-order moving-average low-pass filter $b[0]=b[1]=0.5$.  Each candidate record uses the zero-state convention $X[-1]=0$.  Within a record, the delay stores the preceding sample.  Between records, at least one clock with \texttt{SampleInValid} deasserted inserts zero into the one-sample delay and inserts a deasserted valid tag for that corresponding delayed transaction; an earlier pipeline transaction may still emerge on the same clock.  The next record therefore begins from zero state.

\begin{figure}[H]
\begin{center}
\resizebox{\textwidth}{!}{\input{../images/diagrams/tikz/complex_fir.tex}}
\caption{Configured two-tap complex low-pass filter with coefficients $[0.5,0.5]$.}
\label{fig:cfir}
\end{center}
\end{figure}

\begin{figure}[H]
\begin{center}
\resizebox{0.96\textwidth}{!}{\input{../images/diagrams/tikz/timing_fir.tex}}
\caption{Symbolic continuous-rate timing of the two-tap low-pass filter module. Data, valid, and final-sample tags remain aligned.}
\label{fig:3_FIR}
\end{center}
\end{figure}

\begin{table}[H]
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{center}
\caption{Configured low-pass-filter interface.}
\setlength{\tabcolsep}{4pt}
\begin{tabularx}{\textwidth}{|L{0.24\textwidth}|L{0.13\textwidth}|L{0.28\textwidth}|Y|}
\hline
\textbf{Port Name} & \textbf{Direction} & \textbf{Data Type} & \textbf{Description} \\
\hline
TAP\_COUNT     & Config. & natural & Number of coefficients. \\
\hline
TAP\_WIDTH     & Config. & natural & Coefficient width: one sign bit and TAP\_WIDTH-1 fractional bits. \\
\hline
BIT\_WIDTH     & Config. & natural & Sample width: one sign bit and BIT\_WIDTH-1 fractional bits. \\
\hline
Clock          & Input & std\_logic & Synchronous clock. \\
\hline
Reset          & Input & std\_logic & Synchronous, active-high reset. \\
\hline
Coeffs         & Input & CoeffArray(\newline TAP\_COUNT-1 \newline downto 0) & TAP\_COUNT signed coefficients, with $b[0]=b[1]=0.5$ in this application. \\
\hline
SampleOutReady & Input & std\_logic & Downstream status input; held high during continuous-rate operation. \\
\hline
SampleInValid  & Input & std\_logic & High when input is valid. \\
\hline
SampleInLast   & Input & std\_logic & End-of-record tag asserted with the final input sample. \\
\hline
SampleIn\_I    & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Real component of sample. \\
\hline
SampleIn\_Q    & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Imaginary component of sample. \\
\hline
SampleInReady  & Output & std\_logic & Latency-aligned upstream status for the continuous-rate interface. \\
\hline
SampleOutValid & Output & std\_logic & High when output is valid. \\
\hline
SampleOutLast  & Output & std\_logic & End-of-record tag aligned with the associated output. \\
\hline
SampleOut\_I   & Output & signed(BIT\_WIDTH-1 \newline downto 0) & Real component of filtered sample. \\
\hline
SampleOut\_Q   & Output & signed(BIT\_WIDTH-1 \newline downto 0) & Imaginary component of filtered sample. \\
\hline
\end{tabularx}
\end{center}
\end{table}

%
% 3.3.6
%
\bigskip
\addcontentsline{toc}{subsection}{\quad 3.3.6 \quad Instantaneous Power}
\noindent 3.3.6  \quad Instantaneous Power

% purpose
This module computes the pointwise magnitude squared of a complex sample.  It supplies the $\tau=0$ product $X[n]X^{*}[n]$ used by the zero-lag AF and CAF:

\begin{align*}
P_{X}[n] &= X[n]X^{*}[n] \\
          &= |X[n]|^{2} \\
          &= \Re\{X[n]\}^{2}+\Im\{X[n]\}^{2}.
\end{align*}

The real and imaginary components are squared in parallel and then added.  This pointwise identity is not Parseval's theorem, which instead relates a sum or integral of squared magnitudes in the time domain to one in the frequency domain.  For compactness, the following arithmetic-module tables use the documentation labels LEN\_A, LEN\_B, TRUNC, and RND for the VHDL generics \texttt{LengthA}, \texttt{LengthB}, \texttt{BitstoTrunc}, and \texttt{BitstoRound}, respectively.

\begin{figure}[H]
\begin{center}
\resizebox{\textwidth}{!}{\input{../images/diagrams/tikz/mag_sq.tex}}
\caption{Instantaneous-power architecture with a nonnegative fixed-point output.}
\label{fig:mag_sq}
\end{center}
\end{figure}

\begin{figure}[H]
\begin{center}
\resizebox{0.96\textwidth}{!}{\input{../images/diagrams/tikz/timing_power.tex}}
\caption{Symbolic continuous-rate timing of the instantaneous-power module. Each accepted complex input produces one nonnegative output after fixed latency.}
\label{fig:2_InstPwr}
\end{center}
\end{figure}

\begin{table}[H]
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{center}
\caption{Configured instantaneous-power interface.}
\begin{tabularx}{\textwidth}{|L{0.24\textwidth}|L{0.13\textwidth}|L{0.28\textwidth}|Y|}
\hline
\textbf{Port Name} & \textbf{Direction} & \textbf{Data Type} & \textbf{Description} \\
\hline

LEN\_A         & Generic & natural & Bit width of the real input. \\
\hline
LEN\_B         & Generic & natural & Bit width of the imaginary input. \\
\hline
LATENCY        & Generic & natural & Controls the total operation latency. \\
\hline
TRUNC          & Generic & natural & Number of most-significant bits truncated. \\
\hline
RND            & Generic & natural & Number of least-significant bits removed after rounding. \\
\hline
Clock          & Input & std\_logic & Synchronous clock. \\
\hline
Reset          & Input & std\_logic & Synchronous, active-high reset. \\
\hline
SampleOutReady & Input & std\_logic & Downstream status input; held high during continuous-rate operation. \\
\hline
SampleInValid  & Input & std\_logic & High when input is valid. \\
\hline
SampleInLast   & Input & std\_logic & End-of-record tag asserted with the final input sample. \\
\hline
SampleIn\_A    & Input & signed(LEN\_A-1 \newline downto 0) & Real component of sample.  \\
\hline
SampleIn\_B    & Input & signed(LEN\_B-1 \newline downto 0) & Imaginary component of sample.  \\
\hline
SampleInReady  & Output & std\_logic & Latency-aligned upstream status for the continuous-rate interface. \\
\hline
SampleOutValid & Output & std\_logic & High when output is valid. \\
\hline
SampleOutLast  & Output & std\_logic & End-of-record tag aligned with the associated output. \\
\hline
SampleOut      & Output & unsigned(\newline 2*max(LEN\_A, \newline LEN\_B)-1-RND- \newline TRUNC downto 0) & Nonnegative magnitude-squared output. \\
\hline
\end{tabularx}
\end{center}
\end{table}

%
% 3.3.7
%
\bigskip
\addcontentsline{toc}{subsection}{\quad 3.3.7 \quad Complex Multiplier}
\noindent 3.3.7  \quad Complex Multiplier

The product of two complex numbers can be expressed in terms of pairs of real and imaginary components as follows:

\begin{align*}
X_{1}[n]X_{2}[n]          &= (A+jB)(C+jD) \\
\Re \{X_{1}[n]X_{2}[n] \} &= AC-BD \\
\Im \{X_{1}[n]X_{2}[n] \} &= AD+BC.
\end{align*}

Here, the multiplication of complex values $X_{1}[n]X_{2}[n]$ simplifies to four real multiplications, one real addition, and one real subtraction.

\begin{figure}[H]
\begin{center}
\resizebox{\textwidth}{!}{\input{../images/diagrams/tikz/cmult.tex}}
\caption{Complex-multiplier architecture with explicit real and imaginary arithmetic.}
\label{fig:cmultiplier}
\end{center}
\end{figure}

\begin{figure}[H]
\begin{center}
\resizebox{0.96\textwidth}{!}{\input{../images/diagrams/tikz/timing_cmult.tex}}
\caption{Symbolic continuous-rate timing of the complex-multiplier module. Each aligned input pair produces one complex product.}
\label{fig:1_ComplexMult}
\end{center}
\end{figure}

\begin{table}[H]
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{center}
\caption{Configured complex-multiplier interface.}
\begin{tabularx}{\textwidth}{|L{0.24\textwidth}|L{0.13\textwidth}|L{0.28\textwidth}|Y|}
\hline
\textbf{Port Name} & \textbf{Direction} & \textbf{Data Type} & \textbf{Description} \\
\hline

LEN\_A          & Generic & natural & Operand-A component width. \\
\hline
LEN\_B          & Generic & natural & Operand-B component width. \\
\hline
LATENCY         & Generic & natural & Total operation latency. \\
\hline
TRUNC           & Generic & natural & Most-significant bits truncated. \\
\hline
RND             & Generic & natural & Least-significant bits removed after rounding. \\
\hline
Clock           & Input & std\_logic & Synchronous clock. \\
\hline
Reset           & Input & std\_logic & Active-high synchronous reset. \\
\hline
SampleOutReady  & Input & std\_logic & Downstream status input; held high during continuous-rate operation. \\
\hline
SampleInValid   & Input & std\_logic & Input is valid. \\
\hline
SampleInLast    & Input & std\_logic & End-of-record tag asserted with the final input sample. \\
\hline
SampleIn\_I\_A  & Input & signed(LEN\_A-1 \newline downto 0) & Operand-A real component.  \\
\hline
SampleIn\_Q\_A  & Input & signed(LEN\_A-1 \newline downto 0) & Operand-A imaginary component.  \\
\hline
SampleIn\_I\_B  & Input & signed(LEN\_B-1 \newline downto 0) & Operand-B real component.  \\
\hline
SampleIn\_Q\_B  & Input & signed(LEN\_B-1 \newline downto 0) & Operand-B imaginary component.  \\
\hline
SampleInReady   & Output & std\_logic & Latency-aligned upstream status for the continuous-rate interface. \\
\hline
SampleOutValid  & Output & std\_logic & Output is valid. \\
\hline
SampleOutLast   & Output & std\_logic & End-of-record tag aligned with the associated output. \\
\hline
SampleOut\_I    & Output & signed(\newline LEN\_A+LEN\_B-RND- \newline TRUNC downto 0) & Real component of product. \\
\hline
SampleOut\_Q    & Output & signed(\newline LEN\_A+LEN\_B-RND- \newline TRUNC downto 0) & Imaginary component of product. \\
\hline
\end{tabularx}
\end{center}
\end{table}

%
% 3.3.8
%
\bigskip
\addcontentsline{toc}{subsection}{\quad 3.3.8 \quad Multiplier}
\noindent 3.3.8  \quad Multiplier

There are many real multipliers used throughout this design.  The module provides a continuous-rate, fixed-latency pipeline together with explicit bit-width, truncation, saturation, and rounding controls.  Data, valid, and final-sample metadata advance through corresponding delay stages, while downstream ready is held high for the characterized operating mode.  Mathematically, this module performs the multiplication of two real values, expressed as:

\begin{equation}
Y[n]=X_{1}[n]X_{2}[n].
\end{equation}

\begin{figure}[H]
\begin{center}
\resizebox{\textwidth}{!}{\input{../images/diagrams/tikz/rmult.tex}}
\caption{Fixed-point multiplier architecture with explicit rounding and saturation stages.}
\label{fig:multiplier}
\end{center}
\end{figure}

\begin{figure}[H]
\begin{center}
\resizebox{0.96\textwidth}{!}{\input{../images/diagrams/tikz/timing_rmult.tex}}
\caption{Symbolic continuous-rate timing of the fixed-point multiplier module. The product and its valid and final-sample tags share the configured latency.}
\label{fig:0_GenericMult}
\end{center}
\end{figure}

\begin{table}[H]
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{center}
\caption{Configured multiplier interface.}
\begin{tabularx}{\textwidth}{|L{0.24\textwidth}|L{0.13\textwidth}|L{0.28\textwidth}|Y|}
\hline
\textbf{Port Name} & \textbf{Direction} & \textbf{Data Type} & \textbf{Description} \\
\hline
LEN\_A         & Generic & natural & Bit width of operand A. \\
\hline
LEN\_B         & Generic & natural & Bit width of operand B. \\
\hline
LATENCY        & Generic & natural & Controls the total operation latency. \\
\hline
TRUNC          & Generic & natural & Number of most-significant bits truncated. \\
\hline
RND            & Generic & natural & Number of least-significant bits removed after rounding. \\
\hline
Clock          & Input & std\_logic & Synchronous clock. \\
\hline
Reset          & Input & std\_logic & Synchronous, active-high reset. \\
\hline
SampleOutReady & Input & std\_logic & Downstream status input; held high during continuous-rate operation. \\
\hline
SampleInValid  & Input & std\_logic & High when input is valid. \\
\hline
SampleInLast   & Input & std\_logic & End-of-record tag asserted with the final input sample. \\
\hline
SampleIn\_A    & Input & signed(LEN\_A-1 \newline downto 0) & Real-valued operand A.  \\
\hline
SampleIn\_B    & Input & signed(LEN\_B-1 \newline downto 0) & Real-valued operand B.  \\
\hline
SampleInReady  & Output & std\_logic & Latency-aligned upstream status for the continuous-rate interface. \\
\hline
SampleOutValid & Output & std\_logic & High when output is valid. \\
\hline
SampleOutLast  & Output & std\_logic & End-of-record tag aligned with the associated output. \\
\hline
SampleOut      & Output & signed(\newline LEN\_A+LEN\_B-1 \newline -RND-TRUNC downto 0) & Real-valued product. \\
\hline
\end{tabularx}
\end{center}
\end{table}

%
% 3.3.9
%
\Needspace{14\baselineskip}
\bigskip
\addcontentsline{toc}{subsection}{\quad 3.3.9 \quad Accumulator}
\noindent 3.3.9  \quad Accumulator

% purpose
The accumulator implements the running sum used by each single-frequency Fourier evaluation and participates in the integrate-and-dump flow control between sample sets.  For a sequence whose first accepted sample has local index $n=0$, the recurrence is

\begin{equation}
S[n] = \left\{
       \begin{array}{ll}
         X[n],          & n=0, \\
         S[n-1]+X[n],   & n>0
      \end{array}
      \right.
      .
\end{equation}

\noindent Here, $X[n]$ is an input sample and $S[n]$ is the running sum within the current record.  The module itself is real-valued; the DFT instantiates separate accumulators for the in-phase and quadrature components.  On each valid sample, the adder first forms $S_{\mathrm{next}}=S+X[n]$, with the first sample using a cleared stored sum.  An ordinary sample stores $S_{\mathrm{next}}$.  A sample carrying \texttt{SampleInLast} stores no residual value: it emits the normalized $S_{\mathrm{next}}$, asserts the aligned output-valid and final-sample tags, and clears the stored sum for the next record.  This end-of-record convention includes each of the $N$ samples exactly once.

For a signed $W$-bit input and a record of $N$ samples, the full-range accumulator width is

\begin{equation*}
W_{\mathrm{acc}}=W+\ceil{\log_2 N}.
\end{equation*}

A 16-bit, 4096-sample configuration therefore uses a 28-bit sum and a 12-bit normalization shift.  A 16-bit, 65,536-sample configuration uses a 32-bit sum and a 16-bit shift.  For power-of-two $N$, a sign-aware rounding adjustment followed by selection of the corresponding upper bits implements rounded division by $N$; for other record lengths, normalization uses a constant reciprocal or divider with guard bits before the same rounding and saturation stage.  The configured \texttt{N\_SAMPLES} value establishes the counter limit, accumulator width, and normalization factor, while \texttt{SampleInLast} is asserted with count $N-1$ as the protocol-level end-of-record tag.  An invalid input clock leaves the sample-indexed state unchanged and inserts deasserted valid and final-sample tags into the fixed-latency tag path; a previously accepted sample or terminal result may still emerge on that clock according to the pipeline latency.

\begin{figure}[H]
\begin{center}
\resizebox{\textwidth}{!}{\input{../images/diagrams/tikz/accumulator.tex}}
\caption{Block-synchronous accumulator architecture. The final accepted sample is included before the terminal sum is normalized and emitted.}
\label{fig:accumulator}
\end{center}
\end{figure}

\begin{figure}[H]
\begin{center}
\resizebox{0.96\textwidth}{!}{\input{../images/diagrams/tikz/timing_accumulator.tex}}
\caption[Accumulator end-of-record timing.]{Symbolic block-synchronous timing of the accumulator module. The final-sample marker accompanies sample $N-1$, and one normalized terminal result is emitted after that sample has been included.}
\label{fig:4_accumulator}
\end{center}
\end{figure}

\begin{table}[H]
\singlespacing
\small
\setlength{\tabcolsep}{2pt}
\renewcommand{\arraystretch}{1.05}
\begin{center}
\caption{Configured accumulator interface.}
\begin{tabularx}{\textwidth}{|L{0.24\textwidth}|L{0.13\textwidth}|L{0.28\textwidth}|Y|}
\hline
\textbf{Port Name} & \textbf{Direction} & \textbf{Data Type} & \textbf{Description} \\
\hline
N\_SAMPLES     & Generic & natural & Record length used for counting, width, and normalization. \\
\hline
BIT\_WIDTH     & Generic & natural & Sample width: one sign bit and BIT\_WIDTH-1 fractional bits. \\
\hline
Clock          & Input & std\_logic & Synchronous clock. \\
\hline
Reset          & Input & std\_logic & Synchronous, active-high reset. \\
\hline
SampleOutReady & Input & std\_logic & Downstream status input; held high during continuous-rate operation. \\
\hline
SampleInValid  & Input & std\_logic & High when input is valid. \\
\hline
SampleInLast   & Input & std\_logic & End-of-record tag asserted with sample $N-1$. \\
\hline
SampleIn       & Input & signed(BIT\_WIDTH-1 \newline downto 0) & Real-valued input sample. \\
\hline
SampleInReady  & Output & std\_logic & Latency-aligned upstream status for the continuous-rate interface. \\
\hline
SampleOutValid & Output & std\_logic & High when output is valid. \\
\hline
SampleOutLast  & Output & std\_logic & End-of-record tag aligned with the associated output. \\
\hline
SampleOut      & Output & signed(BIT\_WIDTH-1 \newline downto 0) & Normalized completed-record sum containing all $N$ samples. \\
\hline
\end{tabularx}
\end{center}
\end{table}

\clearpage

