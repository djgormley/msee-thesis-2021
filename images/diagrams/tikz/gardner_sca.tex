\begin{tikzpicture}[node distance=6mm and 8mm]
  \node[arch/label] (xin) {$x(t)$};
  \node[dsp/junction, right=4mm of xin] (split) {};
  \node[dsp/mult, above right=7mm and 8mm of split] (mixp) {$\times$};
  \node[dsp/mult, below right=7mm and 8mm of split] (mixm) {$\times$};
  \node[dsp/lpf, right=8mm of mixp] (lpfp) {};
  \node[dsp/lpf, right=8mm of mixm] (lpfm) {};
  \node[arch/smallblock, right=6mm of lpfm] (conj) {conjugate};
  \coordinate (branchmid) at ($(lpfp.east)!0.5!(conj.east)$);
  \node[dsp/mult, right=12mm of branchmid] (prod) {$\times$};
  \node[arch/block, right=8mm of prod] (avg) {$T$-second\\time average};
  \node[dsp/mult, right=7mm of avg] (scale) {$\times$};
  \node[arch/label, right=6mm of scale] (out) {$\widehat S_x^{\alpha}(t,f)$};

  \draw[arch/wire] (xin) -- (split);
  \draw[arch/wire] (split) |- (mixp.west);
  \draw[arch/wire] (split) |- (mixm.west);
  \draw[arch/wire] (mixp) -- (lpfp);
  \draw[arch/wire] (mixm) -- (lpfm);
  \draw[arch/wire] (lpfm) -- (conj);
  \draw[arch/wire] (lpfp.east) -| (prod.north);
  \draw[arch/wire] (conj.east) -| (prod.south);
  \draw[arch/wire] (prod) -- (avg);
  \draw[arch/wire] (avg) -- (scale);
  \draw[arch/wire] (scale) -- (out);

  \node[arch/tinylabel, above=6mm of mixp] {$\mathrm{e}^{-\mathrm{j}2\pi(f+\alpha/2)t}$};
  \node[arch/tinylabel, below=6mm of mixm] {$\mathrm{e}^{-\mathrm{j}2\pi(f-\alpha/2)t}$};
  \node[arch/tinylabel, above=1mm of lpfp] {low-pass $h_{\Delta f}(t)$\\bandwidth $\Delta f$};
  \node[arch/tinylabel, above=1mm of lpfm] {low-pass $h_{\Delta f}(t)$\\bandwidth $\Delta f$};
  \node[arch/tinylabel, above=2mm of scale] {$\Delta f$};
\end{tikzpicture}
