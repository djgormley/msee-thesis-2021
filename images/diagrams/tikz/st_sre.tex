\begin{tikzpicture}[node distance=5mm]
  \node[arch/label] (zin) {$z_k[n]$\\complex};
  \node[arch/block, right=of zin] (pwr) {instantaneous\\power};
  \node[arch/block, right=of pwr] (fmt) {unsigned format\\align and widen};
  \node[dsp/mult, right=of fmt] (mix) {$\times$};
  \node[dsp/reg, align=center, right=of mix] (dft) {$N$-sample\\accumulator};
  \node[arch/smallblock, right=of dft] (norm) {$\div N$};
  \node[arch/label, right=of norm] (out) {$\widehat C_x(\alpha_v,f_k)$\\complex};

  \draw[arch/wire] (zin) -- (pwr);
  \draw[arch/wire] (pwr) -- (fmt);
  \draw[arch/wire] (fmt) -- (mix);
  \draw[arch/wire] (mix) -- (dft);
  \draw[arch/wire] (dft) -- (norm);
  \draw[arch/wire] (norm) -- (out);

  \node[arch/tinylabel, above=8mm of mix] (srw) {symbol-rate control word $\alpha_v$\\$\mathrm{e}^{-\mathrm{j}2\pi\alpha_vn/F_s}$};
  \node[arch/tinylabel, below=8mm of dft] (ctl) {$N$ and final-sample marker};
  \draw[arch/control] (srw) -- (mix.north);
  \draw[arch/control] (ctl) -- (dft.south);
\end{tikzpicture}
